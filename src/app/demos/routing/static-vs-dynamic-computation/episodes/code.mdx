```jsx
export async function generateStaticParams() {
  return [
    { episodeID: '1' },
    { episodeID: '2' },
  ]
}

export default function Page({ params }) {
  return (
    <div>
      <h1>{ p.params.episodeID }</h1>
    </div>
  )
}

export const dynamicParams = false //※
```